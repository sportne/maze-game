plugins {
    id 'mazegame.application-conventions'
    id 'mazegame.quality-conventions'
    id 'mazegame.coverage-conventions'
    id 'mazegame.native-conventions'
}

// Native Image registration is exercised by nativeImage rather than the unit-test JVM.
def coverageExclusions = ['**/nativeimage/**']

tasks.named('jacocoTestReport') {
    classDirectories.setFrom(sourceSets.main.output.asFileTree.matching {
        exclude coverageExclusions
    })
}

tasks.named('jacocoTestCoverageVerification') {
    classDirectories.setFrom(sourceSets.main.output.asFileTree.matching {
        exclude coverageExclusions
    })
}

description = 'LWJGL3 desktop launcher for Maze Game.'

application {
    mainClass = 'io.github.sportne.mazegame.lwjgl3.Lwjgl3Launcher'
}

tasks.named('run') {
    workingDir = rootProject.file('assets')
}

dependencies {
    implementation project(':modules:core')
    implementation libs.gdx.backend.lwjgl3
    compileOnly libs.graal.nativeimage
    runtimeOnly "com.badlogicgames.gdx:gdx-platform:${libs.versions.gdx.get()}:natives-desktop"
}
