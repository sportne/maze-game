package io.github.sportne.mazegame.model.maze;

/**
 * Wall types available to the player.
 *
 * <p>Milestone 1 only has normal blocking walls. Keeping this enum now gives future block types a
 * stable expansion point.
 */
public enum WallType {
  /** A standard wall that blocks mouse movement. */
  NORMAL
}
