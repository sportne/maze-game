/**
 * Grid dimensions and coordinate primitives shared by levels, mazes, input, and layout code.
 *
 * <p>Coordinates are zero-based and measured from the top-left of the maze grid so they map
 * directly to level authoring data and player-facing rows.
 */
package io.github.sportne.mazegame.model.grid;
