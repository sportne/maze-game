/**
 * Render-independent domain model for Maze Game.
 *
 * <p>The model package is split into focused subpackages for grid primitives, level authoring, maze
 * state, and mouse simulation. It intentionally avoids libGDX dependencies so rules can be tested
 * quickly and reused by renderers, launchers, or future tools.
 */
package io.github.sportne.mazegame.model;
