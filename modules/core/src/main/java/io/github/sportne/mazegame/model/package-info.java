/**
 * Render-independent domain model for Maze Game.
 *
 * <p>The model package contains immutable level data, immutable maze state, wall-placement results,
 * and deterministic mouse simulation. It intentionally avoids libGDX dependencies so rules can be
 * tested quickly and reused by renderers, launchers, or future tools.
 */
package io.github.sportne.mazegame.model;
