/**
 * Deterministic mouse simulation results and statuses.
 *
 * <p>Mouse simulation depends only on immutable maze state and level timing so runs can be replayed
 * from the same seed without libGDX or rendering dependencies.
 */
package io.github.sportne.mazegame.model.mouse;
