/**
 * Authored level definitions and catalogs.
 *
 * <p>Level data is immutable and contains static timing, grid, endpoint, and seed values. Player
 * wall placement lives in the maze package so a level definition can be safely reused across
 * attempts.
 */
package io.github.sportne.mazegame.model.level;
