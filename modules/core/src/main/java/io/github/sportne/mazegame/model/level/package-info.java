/**
 * Authored level definitions and catalogs.
 *
 * <p>Level data is immutable and contains static timing, grid, endpoint, fixed-cell, and seed
 * values. Player cell placement lives in the maze package so a level definition can be safely
 * reused across attempts.
 */
package io.github.sportne.mazegame.model.level;
