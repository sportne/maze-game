name: CI

on:
  pull_request:
  push:

permissions:
  contents: read

jobs:
  build:
    name: Quality gate, web artifact, and native image
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v5
        with:
          submodules: true

      - name: Set up GraalVM
        uses: graalvm/setup-graalvm@v1
        with:
          distribution: graalvm
          java-version: "21"
          cache: gradle

      - name: Fetch PMD submodule tags
        run: git -C tools/pmd fetch --tags --force origin

      - name: Cache Playwright browser
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ hashFiles('gradle/libs.versions.toml') }}

      - name: Install Playwright system dependencies
        run: ./gradlew :modules:browser-tests:playwrightInstallDependencies

      - name: Run quality gate, web build, and native image build
        run: ./gradlew qualityGate webBuild nativeImage

      - name: Upload static website
        uses: actions/upload-artifact@v4
        with:
          name: maze-game-static-site
          path: modules/teavm/build/dist/js/webapp
          if-no-files-found: error
          include-hidden-files: true

      - name: Upload browser smoke-test evidence
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: browser-smoke-evidence
          path: modules/browser-tests/build/reports/browser-smoke
          if-no-files-found: ignore
